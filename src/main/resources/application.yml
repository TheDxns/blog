spring:
  main:
    banner-mode: off
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
      
keycloak:
  realm: 'Blog'
  auth-server-url: 'http://localhost:8180/auth'
  resource: 'blog-api'
  ssl-required: none
  use-resource-role-mappings: true
  bearer-only: true